package com.dlts.hrms.web;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;

@Controller
@RequestMapping("index")
public class IndexController {

	/*
	 * @Autowired UserService userService;
	 * 
	 * @RequestMapping("login") public String login( HttpSession session, User
	 * user){ user = userService.login(user); if( user!=null ){
	 * session.setAttribute("user", user); return
	 * "redirect:html/index/index.html"; }else{ return "redirect:/"; } }
	 */

}
