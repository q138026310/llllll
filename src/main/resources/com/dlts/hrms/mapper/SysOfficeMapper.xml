<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dlts.hrms.mapper.SysOfficeMapper">

  <sql id="Base_Column_List">
    id,parent_id,parent_ids,name,sort,area_id,code,type,grade,address,zip_code,master,phone,fax,email,USEABLE,PRIMARY_PERSON,DEPUTY_PERSON,create_by,create_date,update_by,update_date,remarks,del_flag
  </sql>
  
  <select id="getById" parameterType="java.lang.String" resultType="com.dlts.hrms.entity.SysOffice">
    select <include refid="Base_Column_List" /> from sys_office where id = #{id}
  </select>
  
  <select id="get" parameterType="com.dlts.hrms.entity.SysOffice" resultType="com.dlts.hrms.entity.SysOffice">
    select <include refid="Base_Column_List" /> from sys_office  where 1=1 <if test="id!=null">and id=#{id}</if><if test="parentId!=null">and parent_id=#{parentId}</if><if test="parentIds!=null">and parent_ids=#{parentIds}</if><if test="name!=null">and name=#{name}</if><if test="sort!=null">and sort=#{sort}</if><if test="areaId!=null">and area_id=#{areaId}</if><if test="code!=null">and code=#{code}</if><if test="type!=null">and type=#{type}</if><if test="grade!=null">and grade=#{grade}</if><if test="address!=null">and address=#{address}</if><if test="zipCode!=null">and zip_code=#{zipCode}</if><if test="master!=null">and master=#{master}</if><if test="phone!=null">and phone=#{phone}</if><if test="fax!=null">and fax=#{fax}</if><if test="email!=null">and email=#{email}</if><if test="USEABLE!=null">and USEABLE=#{USEABLE}</if><if test="PRIMARYPERSON!=null">and PRIMARY_PERSON=#{PRIMARYPERSON}</if><if test="DEPUTYPERSON!=null">and DEPUTY_PERSON=#{DEPUTYPERSON}</if><if test="createBy!=null">and create_by=#{createBy}</if><if test="createDate!=null">and create_date=#{createDate}</if><if test="updateBy!=null">and update_by=#{updateBy}</if><if test="updateDate!=null">and update_date=#{updateDate}</if><if test="remarks!=null">and remarks=#{remarks}</if><if test="delFlag!=null">and del_flag=#{delFlag}</if>
  </select>
  
  <select id="select" parameterType="com.dlts.hrms.entity.SysOffice" resultType="com.dlts.hrms.entity.SysOffice">
    select <include refid="Base_Column_List" /> from sys_office  where 1=1 <if test="id!=null">and id=#{id}</if><if test="parentId!=null">and parent_id=#{parentId}</if><if test="parentIds!=null">and parent_ids=#{parentIds}</if><if test="name!=null">and name=#{name}</if><if test="sort!=null">and sort=#{sort}</if><if test="areaId!=null">and area_id=#{areaId}</if><if test="code!=null">and code=#{code}</if><if test="type!=null">and type=#{type}</if><if test="grade!=null">and grade=#{grade}</if><if test="address!=null">and address=#{address}</if><if test="zipCode!=null">and zip_code=#{zipCode}</if><if test="master!=null">and master=#{master}</if><if test="phone!=null">and phone=#{phone}</if><if test="fax!=null">and fax=#{fax}</if><if test="email!=null">and email=#{email}</if><if test="USEABLE!=null">and USEABLE=#{USEABLE}</if><if test="PRIMARYPERSON!=null">and PRIMARY_PERSON=#{PRIMARYPERSON}</if><if test="DEPUTYPERSON!=null">and DEPUTY_PERSON=#{DEPUTYPERSON}</if><if test="createBy!=null">and create_by=#{createBy}</if><if test="createDate!=null">and create_date=#{createDate}</if><if test="updateBy!=null">and update_by=#{updateBy}</if><if test="updateDate!=null">and update_date=#{updateDate}</if><if test="remarks!=null">and remarks=#{remarks}</if><if test="delFlag!=null">and del_flag=#{delFlag}</if>
  </select>
  
  <select id="count" parameterType="com.dlts.hrms.entity.SysOffice" resultType="java.lang.Integer">
    select count(1) from sys_office  where 1=1 <if test="id!=null">and id=#{id}</if><if test="parentId!=null">and parent_id=#{parentId}</if><if test="parentIds!=null">and parent_ids=#{parentIds}</if><if test="name!=null">and name=#{name}</if><if test="sort!=null">and sort=#{sort}</if><if test="areaId!=null">and area_id=#{areaId}</if><if test="code!=null">and code=#{code}</if><if test="type!=null">and type=#{type}</if><if test="grade!=null">and grade=#{grade}</if><if test="address!=null">and address=#{address}</if><if test="zipCode!=null">and zip_code=#{zipCode}</if><if test="master!=null">and master=#{master}</if><if test="phone!=null">and phone=#{phone}</if><if test="fax!=null">and fax=#{fax}</if><if test="email!=null">and email=#{email}</if><if test="USEABLE!=null">and USEABLE=#{USEABLE}</if><if test="PRIMARYPERSON!=null">and PRIMARY_PERSON=#{PRIMARYPERSON}</if><if test="DEPUTYPERSON!=null">and DEPUTY_PERSON=#{DEPUTYPERSON}</if><if test="createBy!=null">and create_by=#{createBy}</if><if test="createDate!=null">and create_date=#{createDate}</if><if test="updateBy!=null">and update_by=#{updateBy}</if><if test="updateDate!=null">and update_date=#{updateDate}</if><if test="remarks!=null">and remarks=#{remarks}</if><if test="delFlag!=null">and del_flag=#{delFlag}</if>
  </select>
  
  <delete id="delete" parameterType="java.lang.String">
    delete from sys_office where id = #{id}
  </delete>
  
  <delete id="deleteSelective" parameterType="com.dlts.hrms.entity.SysOffice">
    delete from sys_office  where 1=1 <if test="id!=null">and id=#{id}</if><if test="parentId!=null">and parent_id=#{parentId}</if><if test="parentIds!=null">and parent_ids=#{parentIds}</if><if test="name!=null">and name=#{name}</if><if test="sort!=null">and sort=#{sort}</if><if test="areaId!=null">and area_id=#{areaId}</if><if test="code!=null">and code=#{code}</if><if test="type!=null">and type=#{type}</if><if test="grade!=null">and grade=#{grade}</if><if test="address!=null">and address=#{address}</if><if test="zipCode!=null">and zip_code=#{zipCode}</if><if test="master!=null">and master=#{master}</if><if test="phone!=null">and phone=#{phone}</if><if test="fax!=null">and fax=#{fax}</if><if test="email!=null">and email=#{email}</if><if test="USEABLE!=null">and USEABLE=#{USEABLE}</if><if test="PRIMARYPERSON!=null">and PRIMARY_PERSON=#{PRIMARYPERSON}</if><if test="DEPUTYPERSON!=null">and DEPUTY_PERSON=#{DEPUTYPERSON}</if><if test="createBy!=null">and create_by=#{createBy}</if><if test="createDate!=null">and create_date=#{createDate}</if><if test="updateBy!=null">and update_by=#{updateBy}</if><if test="updateDate!=null">and update_date=#{updateDate}</if><if test="remarks!=null">and remarks=#{remarks}</if><if test="delFlag!=null">and del_flag=#{delFlag}</if>
  </delete>
  
  <insert id="insert" parameterType="com.dlts.hrms.entity.SysOffice">
    insert into sys_area (id,parent_id,parent_ids,name,sort,area_id,code,type,grade,address,zip_code,master,phone,fax,email,USEABLE,PRIMARY_PERSON,DEPUTY_PERSON,create_by,create_date,update_by,update_date,remarks,del_flag) values (#{id},#{parent_id},#{parent_ids},#{name},#{sort},#{area_id},#{code},#{type},#{grade},#{address},#{zip_code},#{master},#{phone},#{fax},#{email},#{USEABLE},#{PRIMARY_PERSON},#{DEPUTY_PERSON},#{create_by},#{create_date},#{update_by},#{update_date},#{remarks},#{del_flag})
  </insert>
  
  <update id="updateSelective" parameterType="com.dlts.hrms.entity.SysOffice">
    update sys_office <set><if test="id != null">id = #{id},</if><if test="parentId != null">parent_id = #{parentId},</if><if test="parentIds != null">parent_ids = #{parentIds},</if><if test="name != null">name = #{name},</if><if test="sort != null">sort = #{sort},</if><if test="areaId != null">area_id = #{areaId},</if><if test="code != null">code = #{code},</if><if test="type != null">type = #{type},</if><if test="grade != null">grade = #{grade},</if><if test="address != null">address = #{address},</if><if test="zipCode != null">zip_code = #{zipCode},</if><if test="master != null">master = #{master},</if><if test="phone != null">phone = #{phone},</if><if test="fax != null">fax = #{fax},</if><if test="email != null">email = #{email},</if><if test="USEABLE != null">USEABLE = #{USEABLE},</if><if test="PRIMARYPERSON != null">PRIMARY_PERSON = #{PRIMARYPERSON},</if><if test="DEPUTYPERSON != null">DEPUTY_PERSON = #{DEPUTYPERSON},</if><if test="createBy != null">create_by = #{createBy},</if><if test="createDate != null">create_date = #{createDate},</if><if test="updateBy != null">update_by = #{updateBy},</if><if test="updateDate != null">update_date = #{updateDate},</if><if test="remarks != null">remarks = #{remarks},</if><if test="delFlag != null">del_flag = #{delFlag},</if></set> where id = #{id}
  </update>
  
  <update id="update" parameterType="com.dlts.hrms.entity.SysOffice">
    update sys_office set id=#{id},parent_id=#{parentId},parent_ids=#{parentIds},name=#{name},sort=#{sort},area_id=#{areaId},code=#{code},type=#{type},grade=#{grade},address=#{address},zip_code=#{zipCode},master=#{master},phone=#{phone},fax=#{fax},email=#{email},USEABLE=#{USEABLE},PRIMARY_PERSON=#{PRIMARYPERSON},DEPUTY_PERSON=#{DEPUTYPERSON},create_by=#{createBy},create_date=#{createDate},update_by=#{updateBy},update_date=#{updateDate},remarks=#{remarks},del_flag=#{delFlag} where id = #{id}
  </update>
  
  <!--gen-e-1-w-0-->
  
  
  
  <!--gen-e-1-w-1-->
  
</mapper>