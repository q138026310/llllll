<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dlts.hrms.mapper.SysCompanyMapper">

  <sql id="Base_Column_List">
    id,name,custom_code,code,parent_code,contact,contact_phone,create_user_id,create_time,update_user_id,update_time,delete_flag
  </sql>
  
  <insert id="insert" parameterType="com.dlts.hrms.entity.SysCompany">
    insert into sys_company (id,name,custom_code,code,parent_code,contact,contact_phone,create_user_id,create_time,update_user_id,update_time,delete_flag) values (#{id},#{name},#{customCode},#{code},#{parentCode},#{contact},#{contactPhone},#{createUserId},#{createTime},#{updateUserId},#{updateTime},#{deleteFlag})
  </insert>
  
  <update id="updateSelective" parameterType="com.dlts.hrms.entity.SysCompany">
    update sys_company <set><if test="id != null">id = #{id},</if><if test="name != null">name = #{name},</if><if test="customCode != null">custom_code = #{customCode},</if><if test="code != null">code = #{code},</if><if test="parentCode != null">parent_code = #{parentCode},</if><if test="contact != null">contact = #{contact},</if><if test="contactPhone != null">contact_phone = #{contactPhone},</if><if test="createUserId != null">create_user_id = #{createUserId},</if><if test="createTime != null">create_time = #{createTime},</if><if test="updateUserId != null">update_user_id = #{updateUserId},</if><if test="updateTime != null">update_time = #{updateTime},</if><if test="deleteFlag != null">delete_flag = #{deleteFlag},</if></set> where id = #{id}
  </update>
  
  <update id="update" parameterType="com.dlts.hrms.entity.SysCompany">
    update sys_company set id=#{id},name=#{name},custom_code=#{customCode},code=#{code},parent_code=#{parentCode},contact=#{contact},contact_phone=#{contactPhone},create_user_id=#{createUserId},create_time=#{createTime},update_user_id=#{updateUserId},update_time=#{updateTime},delete_flag=#{deleteFlag} where id = #{id}
  </update>
  
  <delete id="delete" parameterType="java.lang.String">
    delete from sys_company where id = #{id}
  </delete>
  
  <update id="deleteLogic" parameterType="java.lang.String">
    update sys_company set delete_flag = 0 where id = #{id}
  </update>
  
  <delete id="deleteSelective" parameterType="com.dlts.hrms.entity.SysCompany">
    delete from sys_company  where 1=1 <if test="id!=null">and id=#{id}</if><if test="name!=null">and name=#{name}</if><if test="customCode!=null">and custom_code=#{customCode}</if><if test="code!=null">and code=#{code}</if><if test="parentCode!=null">and parent_code=#{parentCode}</if><if test="contact!=null">and contact=#{contact}</if><if test="contactPhone!=null">and contact_phone=#{contactPhone}</if><if test="createUserId!=null">and create_user_id=#{createUserId}</if><if test="createTime!=null">and create_time=#{createTime}</if><if test="updateUserId!=null">and update_user_id=#{updateUserId}</if><if test="updateTime!=null">and update_time=#{updateTime}</if><if test="deleteFlag!=null">and delete_flag=#{deleteFlag}</if>
  </delete>
  
  <select id="getById" parameterType="java.lang.String" resultType="com.dlts.hrms.entity.SysCompany">
    select <include refid="Base_Column_List" /> from sys_company where id = #{id}
  </select>
  
  <select id="get" parameterType="com.dlts.hrms.entity.SysCompany" resultType="com.dlts.hrms.entity.SysCompany">
    select <include refid="Base_Column_List" /> from sys_company  where 1=1 <if test="id!=null">and id=#{id}</if><if test="name!=null">and name=#{name}</if><if test="customCode!=null">and custom_code=#{customCode}</if><if test="code!=null">and code=#{code}</if><if test="parentCode!=null">and parent_code=#{parentCode}</if><if test="contact!=null">and contact=#{contact}</if><if test="contactPhone!=null">and contact_phone=#{contactPhone}</if><if test="createUserId!=null">and create_user_id=#{createUserId}</if><if test="createTime!=null">and create_time=#{createTime}</if><if test="updateUserId!=null">and update_user_id=#{updateUserId}</if><if test="updateTime!=null">and update_time=#{updateTime}</if><if test="deleteFlag!=null">and delete_flag=#{deleteFlag}</if>
  </select>
  
  <select id="select" parameterType="com.dlts.hrms.entity.SysCompany" resultType="com.dlts.hrms.entity.SysCompany">
    select <include refid="Base_Column_List" /> from sys_company  where 1=1 <if test="id!=null">and id=#{id}</if><if test="name!=null">and name=#{name}</if><if test="customCode!=null">and custom_code=#{customCode}</if><if test="code!=null">and code=#{code}</if><if test="parentCode!=null">and parent_code=#{parentCode}</if><if test="contact!=null">and contact=#{contact}</if><if test="contactPhone!=null">and contact_phone=#{contactPhone}</if><if test="createUserId!=null">and create_user_id=#{createUserId}</if><if test="createTime!=null">and create_time=#{createTime}</if><if test="updateUserId!=null">and update_user_id=#{updateUserId}</if><if test="updateTime!=null">and update_time=#{updateTime}</if><if test="deleteFlag!=null">and delete_flag=#{deleteFlag}</if>
  </select>
  
  <select id="count" parameterType="com.dlts.hrms.entity.SysCompany" resultType="java.lang.Integer">
    select count(1) from sys_company  where 1=1 <if test="id!=null">and id=#{id}</if><if test="name!=null">and name=#{name}</if><if test="customCode!=null">and custom_code=#{customCode}</if><if test="code!=null">and code=#{code}</if><if test="parentCode!=null">and parent_code=#{parentCode}</if><if test="contact!=null">and contact=#{contact}</if><if test="contactPhone!=null">and contact_phone=#{contactPhone}</if><if test="createUserId!=null">and create_user_id=#{createUserId}</if><if test="createTime!=null">and create_time=#{createTime}</if><if test="updateUserId!=null">and update_user_id=#{updateUserId}</if><if test="updateTime!=null">and update_time=#{updateTime}</if><if test="deleteFlag!=null">and delete_flag=#{deleteFlag}</if>
  </select>
  
  <!--gen-e-1-w-0-->
  <select id="pageCount" parameterType="map" resultType="java.lang.Integer">
    select count(1) from sys_company t where 1=1 
    <include refid="pageWhere"></include>
  </select>
  
  <select id="pageSelect" parameterType="map" resultType="com.dlts.hrms.po.CompanyPo">
    select t.*,(select name from sys_company a where a.code = t.parent_code) parentName
    from sys_company t where 1=1 
    <include refid="pageWhere"></include>
    order by t.create_time desc,id
    limit #{start},#{limit}
  </select>
  
  <sql id="pageWhere">
  	and t.code like concat(#{loginCompanyCode},'%') 
  	<if test="name!=null and name!=''">
    	and t.name LIKE CONCAT('%',#{name},'%')
    </if>
    <if test="customCode!=null and customCode!=''">
    	and t.custom_code LIKE CONCAT('%',#{customCode},'%')
    </if>
  </sql>
  
  <select id="authCount" parameterType="map" resultType="java.lang.Integer">
  	select count(1) from sys_company t where t.code like concat(#{loginCompanyCode},'%') 
  	<if test="code!=null and code!=''">
    	and t.code = #{code}
    </if>
    <if test="customCode!=null and customCode!=''">
    	and t.custom_code = #{customCode}
    </if>
    <if test="name!=null and name!=''">
    	and t.name = #{name}
    </if>
  </select>
  
  <insert id="insertTree" parameterType="com.dlts.hrms.entity.SysCompany">
    insert into sys_company (id,name,custom_code,code,parent_code,contact,contact_phone,create_user_id,create_time,update_user_id,update_time) 
    values (#{id},#{name},#{customCode},
    ifnull((select a.code from (select max(code)+1 code from sys_company where parent_code = #{parentCode}) a),concat(#{parentCode},'001')),
    #{parentCode},#{contact},#{contactPhone},#{createUserId},#{createTime},#{updateUserId},#{updateTime})
  </insert>
  <!--gen-e-1-w-1-->
  
</mapper>