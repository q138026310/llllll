<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dlts.hrms.mapper.SysUserMapper">

  <sql id="Base_Column_List">
    id,company_id,office_id,username,password,no,name,email,phone,mobile,user_type,photo,login_ip,login_date,login_flag,create_by,create_date,update_by,update_date,remarks,del_flag
  </sql>
  
  <select id="getById" parameterType="java.lang.String" resultType="com.dlts.hrms.entity.SysUser">
    select <include refid="Base_Column_List" /> from sys_user where id = #{id}
  </select>
  
  <select id="get" parameterType="com.dlts.hrms.entity.SysUser" resultType="com.dlts.hrms.entity.SysUser">
    select <include refid="Base_Column_List" /> from sys_user  where 1=1 <if test="id!=null">and id=#{id}</if><if test="companyId!=null">and company_id=#{companyId}</if><if test="officeId!=null">and office_id=#{officeId}</if><if test="username!=null">and username=#{username}</if><if test="password!=null">and password=#{password}</if><if test="no!=null">and no=#{no}</if><if test="name!=null">and name=#{name}</if><if test="email!=null">and email=#{email}</if><if test="phone!=null">and phone=#{phone}</if><if test="mobile!=null">and mobile=#{mobile}</if><if test="userType!=null">and user_type=#{userType}</if><if test="photo!=null">and photo=#{photo}</if><if test="loginIp!=null">and login_ip=#{loginIp}</if><if test="loginDate!=null">and login_date=#{loginDate}</if><if test="loginFlag!=null">and login_flag=#{loginFlag}</if><if test="createBy!=null">and create_by=#{createBy}</if><if test="createDate!=null">and create_date=#{createDate}</if><if test="updateBy!=null">and update_by=#{updateBy}</if><if test="updateDate!=null">and update_date=#{updateDate}</if><if test="remarks!=null">and remarks=#{remarks}</if><if test="delFlag!=null">and del_flag=#{delFlag}</if>
  </select>
  
  <select id="select" parameterType="com.dlts.hrms.entity.SysUser" resultType="com.dlts.hrms.entity.SysUser">
    select <include refid="Base_Column_List" /> from sys_user  where 1=1 <if test="id!=null">and id=#{id}</if><if test="companyId!=null">and company_id=#{companyId}</if><if test="officeId!=null">and office_id=#{officeId}</if><if test="username!=null">and username=#{username}</if><if test="password!=null">and password=#{password}</if><if test="no!=null">and no=#{no}</if><if test="name!=null">and name=#{name}</if><if test="email!=null">and email=#{email}</if><if test="phone!=null">and phone=#{phone}</if><if test="mobile!=null">and mobile=#{mobile}</if><if test="userType!=null">and user_type=#{userType}</if><if test="photo!=null">and photo=#{photo}</if><if test="loginIp!=null">and login_ip=#{loginIp}</if><if test="loginDate!=null">and login_date=#{loginDate}</if><if test="loginFlag!=null">and login_flag=#{loginFlag}</if><if test="createBy!=null">and create_by=#{createBy}</if><if test="createDate!=null">and create_date=#{createDate}</if><if test="updateBy!=null">and update_by=#{updateBy}</if><if test="updateDate!=null">and update_date=#{updateDate}</if><if test="remarks!=null">and remarks=#{remarks}</if><if test="delFlag!=null">and del_flag=#{delFlag}</if>
  </select>
  
  <delete id="delete" parameterType="java.lang.String">
    delete from sys_user where id = #{id}
  </delete>
  
  <delete id="deleteSelective" parameterType="com.dlts.hrms.entity.SysUser">
    delete from sys_user  where 1=1 <if test="id!=null">and id=#{id}</if><if test="companyId!=null">and company_id=#{companyId}</if><if test="officeId!=null">and office_id=#{officeId}</if><if test="username!=null">and username=#{username}</if><if test="password!=null">and password=#{password}</if><if test="no!=null">and no=#{no}</if><if test="name!=null">and name=#{name}</if><if test="email!=null">and email=#{email}</if><if test="phone!=null">and phone=#{phone}</if><if test="mobile!=null">and mobile=#{mobile}</if><if test="userType!=null">and user_type=#{userType}</if><if test="photo!=null">and photo=#{photo}</if><if test="loginIp!=null">and login_ip=#{loginIp}</if><if test="loginDate!=null">and login_date=#{loginDate}</if><if test="loginFlag!=null">and login_flag=#{loginFlag}</if><if test="createBy!=null">and create_by=#{createBy}</if><if test="createDate!=null">and create_date=#{createDate}</if><if test="updateBy!=null">and update_by=#{updateBy}</if><if test="updateDate!=null">and update_date=#{updateDate}</if><if test="remarks!=null">and remarks=#{remarks}</if><if test="delFlag!=null">and del_flag=#{delFlag}</if>
  </delete>
  
  <insert id="insert" parameterType="com.dlts.hrms.entity.SysUser">
    insert into sys_area (id,company_id,office_id,username,password,no,name,email,phone,mobile,user_type,photo,login_ip,login_date,login_flag,create_by,create_date,update_by,update_date,remarks,del_flag) values (#{id},#{company_id},#{office_id},#{username},#{password},#{no},#{name},#{email},#{phone},#{mobile},#{user_type},#{photo},#{login_ip},#{login_date},#{login_flag},#{create_by},#{create_date},#{update_by},#{update_date},#{remarks},#{del_flag})
  </insert>
  
  <update id="updateSelective" parameterType="com.dlts.hrms.entity.SysUser">
    update sys_user <set><if test="id != null">id = #{id},</if><if test="companyId != null">company_id = #{companyId},</if><if test="officeId != null">office_id = #{officeId},</if><if test="username != null">username = #{username},</if><if test="password != null">password = #{password},</if><if test="no != null">no = #{no},</if><if test="name != null">name = #{name},</if><if test="email != null">email = #{email},</if><if test="phone != null">phone = #{phone},</if><if test="mobile != null">mobile = #{mobile},</if><if test="userType != null">user_type = #{userType},</if><if test="photo != null">photo = #{photo},</if><if test="loginIp != null">login_ip = #{loginIp},</if><if test="loginDate != null">login_date = #{loginDate},</if><if test="loginFlag != null">login_flag = #{loginFlag},</if><if test="createBy != null">create_by = #{createBy},</if><if test="createDate != null">create_date = #{createDate},</if><if test="updateBy != null">update_by = #{updateBy},</if><if test="updateDate != null">update_date = #{updateDate},</if><if test="remarks != null">remarks = #{remarks},</if><if test="delFlag != null">del_flag = #{delFlag},</if></set> where id = #{id}
  </update>
  
  <update id="update" parameterType="com.dlts.hrms.entity.SysUser">
    update sys_user set id=#{id},company_id=#{companyId},office_id=#{officeId},username=#{username},password=#{password},no=#{no},name=#{name},email=#{email},phone=#{phone},mobile=#{mobile},user_type=#{userType},photo=#{photo},login_ip=#{loginIp},login_date=#{loginDate},login_flag=#{loginFlag},create_by=#{createBy},create_date=#{createDate},update_by=#{updateBy},update_date=#{updateDate},remarks=#{remarks},del_flag=#{delFlag} where id = #{id}
  </update>
  
</mapper>